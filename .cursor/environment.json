{
  "name": "TradVue",
  "install": "cd backend && npm ci && cd ../frontend && npm ci",
  "terminals": [
    {
      "name": "backend",
      "command": "cd backend && NODE_ENV=development PORT=3001 JWT_SECRET=${JWT_SECRET:-dev_local_secret_change_me} node server.js"
    },
    {
      "name": "frontend",
      "command": "cd frontend && NEXT_PUBLIC_API_URL=http://localhost:3001 npm run dev"
    }
  ],
  "ports": [
    { "name": "frontend", "port": 3000 },
    { "name": "backend", "port": 3001 }
  ]
}
